import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h1>Why are you using firebase? What other options do you have to implement authentication?</h1>
            <h2>== Firebase usually used for authentication system. For authentication, there are several options such as STYTCH.
                Ory.
                Supabase.
                Okta.
                PingIdentity.
                Keycloak.
                Frontegg.
                Authress.</h2>

            <h1>What other services does firebase provide other than authentication?</h1>
            <h2>==The core features of Firebase include NoSQL databases, real-time queries, scalable hosting, data storage, file storage, REST APIs, machine learning, and analytics.</h2>
        </div>
    );
};

export default Blogs;